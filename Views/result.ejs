<!DOCTYPE html>
<html>
<head>
  <title>Success</title>
  <style>
 body {
  margin: 0;
  padding: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  background: linear-gradient(135deg, #e8e1ff, #d4c6ff); /* Soft lavender gradient */
  font-family: 'Poppins', sans-serif;
}


    .container {
      width: 380px;
      padding: 35px;
  border-radius: 25px;
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(18px);
  border: 1px solid rgba(255, 255, 255, 0.5);
  box-shadow: 0 25px 50px rgba(90, 60, 150, 0.25);
  animation: fadeIn 0.6s ease-in-out;
    }

    h2 {
      margin-bottom: 25px;
      color: #0b0a0a;
      font-size: 26px;
      font-weight: 500;
    }

    p {
      color: #333;
      font-size: 16px;
      margin: 15px 0;
      text-align: left;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 24px;
      background: linear-gradient(135deg, #4b79a1, #283e51);
      color: white;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      transition: 0.3s;
    }

    a:hover {
      transform: translateY(-2px);
      background: linear-gradient(135deg, #395e79, #1d2e3a);
    }
  </style>
</head>

<body>
  <div class="container">
    <h2>Submission Successful</h2>
    
    <p><strong>Name:</strong> <%= name %></p>
    <p><strong>Email:</strong> <%= email %></p>
    <p><strong>Department:</strong> <%= department %></p>
    <p><strong>Gender:</strong> <%= gender %></p>

    <a href="/">Submit Another</a>
    <a href="/responses">View All Responses</a>
  </div>

</body>
</html>