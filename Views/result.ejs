<!DOCTYPE html>
<html>
<head>
  <title>Success</title>
  <link rel="stylesheet" href="/output.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
 body {
  margin: 0;
  padding: 10px;
  display: flex;
  justify-content: center;
  align-items: center;
  background: radial-gradient(circle, rgba(209, 130, 163, 1) 23%, rgba(148, 187, 233, 1) 100%);
  font-family: 'Inter', sans-serif;
}


    .container {
      width: 380px;
      padding: 35px;
  border-radius: 25px;
  background: rgba(255, 255, 255, 0.65);
  backdrop-filter: blur(18px);
  border: 1px solid rgba(255, 255, 255, 0.5);
  box-shadow: 0 25px 50px rgba(90, 60, 150, 0.25);
  animation: fadeIn 0.6s ease-in-out;
    }

    h2 {
      margin-bottom: 25px;
      color: #0b0a0a;
      font-size: 26px;
      font-weight: 500;
      font-family: 'Poppins', sans-serif;
    }

    p {
      color: #333;
      font-size: 16px;
      margin: 15px 0;
      text-align: left;
    }

    a {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 24px;
      color: white;
      text-decoration: none;
      border-radius: 12px;
      font-weight: 600;
      transition: 0.3s;
      font-family: 'Inter', sans-serif;
    }

    a:hover {
      transform: translateY(-2px);
    }
    
    .btn-primary {
      background: #d182a3;
    }
    
    .btn-primary:hover {
      background: #c16d92;
    }
    
    .btn-secondary {
      background: #94bbe9;
    }
    
    .btn-secondary:hover {
      background: #7ba8e0;
    }
  </style>
</head>

<body>
  <div class="container">
    <h2>Submission Successful</h2>
    
    <p><strong>Name:</strong> <%= name %></p>
    <p><strong>Email:</strong> <%= email %></p>
    <p><strong>Department:</strong> <%= department %></p>
    <p><strong>Gender:</strong> <%= gender %></p>

    <a href="/" class="btn-primary">Submit Another</a>
    <a href="/responses" class="btn-secondary">View All Responses</a>
  </div>

</body>
</html>